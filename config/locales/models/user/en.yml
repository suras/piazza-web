en:
   activerecord: 
      errors:
        models:
           user:
              attributes:
                 email:
                    blank: "cannot be blank"
                    invalid: "is invalid"
                 password:
                    blank: "cannot be blank"
                    too_short: "is too short (minimum is 8 characters required)%"
                 password_confirmation:
                    confirmation: "Password did't match with confirmation"
                 password_challenge:
                    invalid: "Password incorrect"
